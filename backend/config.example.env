# Server Monitor Agent - Configuração

# Porta do servidor WebSocket
PORT=8765

# Senha para autenticação (será hashada com SHA256)
PASSWORD=admin123

# Caminho do banco de dados SQLite
DB_PATH=/var/lib/server-monitor/metrics.db

# Intervalo de coleta de métricas (em segundos)
COLLECT_INTERVAL=5

# Host para bind (0.0.0.0 = todas as interfaces)
HOST=0.0.0.0
